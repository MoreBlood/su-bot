version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm install --production
    post-build:
      - npm run build
run:
  runtime-version: 16.13.2
  command: npm start
  network:
    port: 8080
    env: APP_PORT
